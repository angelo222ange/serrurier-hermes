import { ReactNode } from "react";

/**
 * Layout pour les routes /serrurier-bordeaux/[service]/
 * Hérite du layout parent /serrurier-bordeaux/
 */
export default function SerrurierBordeauxServiceLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
